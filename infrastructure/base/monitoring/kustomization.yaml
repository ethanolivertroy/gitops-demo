apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
  - namespace.yaml
  - helmrepository.yaml
  - prometheus-helmrelease.yaml
  - loki-helmrelease.yaml
  - grafana-helmrelease.yaml
  - compliance-dashboard.yaml
  # Flux notifications need to be applied to flux-system namespace
  # Apply separately: kubectl apply -f flux-notifications.yaml
